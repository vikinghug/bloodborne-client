$pseudo-base {
  content: '';
  position: absolute;
  left: 30px;
}

triangle-color = $red;

.vote {
  text-align: center;

  &:after {
    @extend $pseudo-base;
    border: 10px solid transparent;
    border-top-color: triangle-color;
    top: 44px;
  }

  &:before {
    @extend $pseudo-base;
    border: 10px solid transparent;
    border-bottom-color: triangle-color;
    top: 0;
  }
}
